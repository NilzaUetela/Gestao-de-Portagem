name: smart_portagem
description: "A new Flutter project."
# Prevents the package from being accidentally published to pub.dev
publish_to: 'none' 

# Version and build number
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Pacote para ícones estilo iOS
  cupertino_icons: ^1.0.8

  # Pacote para formatação de datas, números, moedas etc.
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Lints para boas práticas
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Exemplo de como adicionar assets futuramente:
  # assets:
  #   - images/
  #
  # Exemplo de como adicionar fontes futuramente:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
